{
  /*
  フォロー中ボタンHover時にテキストと色を変更する関数

  const follow_btn = document.getElementById("follow_btn")

  follow_btn.addEventListener('mouseover', function(){
    if(follow_btn.textContent === "フォロー中"){
      follow_btn.classList.replace("btn-primary", "btn-danger")
      follow_btn.textContent = "フォロー解除"
    }
  })

  follow_btn.addEventListener('mouseleave', function(){
    if(follow_btn.textContent === "フォロー解除"){
      follow_btn.classList.replace("btn-danger", "btn-primary")
      follow_btn.textContent = "フォロー中"
    }
  })
  */
}