<%
   this_user = @this_user
%>
<div class="main profile">
   <%= render 'shared/profile', this_user: this_user %>
</div>

<%= render partial: 'shared/TimeLine', locals:{ 'posts': @posts, 'users': @users } %>